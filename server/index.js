import express from 'express';import bodyParser from 'body-parser';import cors from 'cors';const app = express();const PORT = 5000; // You can use any available portapp.use(bodyParser.json());app.use(cors());// Simulated user data stored in memorylet users= [];// Add a userapp.post('/users', (req, res) => {    const newUser = { ...req.body, id: users.length + 1 };    users.push(newUser);    res.status(201).json(newUser);});// Update checkoutDate for a userapp.post('/checkout/:userId', (req, res) => {    const userId = parseInt(req.params.userId);    const userIndex = users.findIndex((user) => user.id === userId);    if (userIndex !== -1) {        const updatedUser = { ...users[userIndex], checkoutDate: new Date().toISOString() };        users[userIndex] = updatedUser;        res.json(updatedUser);    } else {        res.status(404).json({ message: 'User not found' });    }});app.listen(PORT, () => {    console.log(`Server is running on port ${PORT}`);});