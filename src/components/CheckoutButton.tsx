import React from 'react';import axios from 'axios';import { IHotelGuest } from '../types';interface CheckoutButtonProps {    user: HotelGuest;    onUpdateCheckout: (updatedUser: IHotelGuest) => void;}const CheckoutButton: React.FC<CheckoutButtonProps> = ({ user, onUpdateCheckout }) => {    const handleCheckout = async () => {        const updatedUser = { ...user, checkoutDate: new Date().toISOString() };        try {            const response = await axios.post('YOUR_CHECKOUT_API_ENDPOINT', updatedUser);            console.log('Checkout successful:', response.data);            onUpdateCheckout(updatedUser);        } catch (error) {            console.error('Error during checkout:', error);        }    };    return <button className="checkout-button" onClick={handleCheckout}>Checkout</button>;};export default CheckoutButton;